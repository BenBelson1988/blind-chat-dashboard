import styled from "styled-components";

export const NavbarLogo = styled.img`
  width: 70px;
  position: absolute;
  left: 40px;
  cursor: pointer;
  transition: ease-in-out 0.5s;
  :hover {
    transform: scale(1.5);
  }
`;
